<h1><?php echo $this->questionnaire->getQuestionnaireTitle()->text; ?></h1>

<a id="edit_questionnaire" class="edit with_icon" href="#"><?php echo t('edit questionnaire properties'); ?></a>
<br clear="all" />
<?php echo $this->form ?>
<br clear="all" />

<div class="clear">
    <a class="ajax link add" href="<?php echo $this->baseUrl('/question/add/questionnaire_id/' . $this->questionnaire['id']) ?>" title="<?php echo t('add new question'); ?>">
        <?php echo t('add new question'); ?>
    </a>
</div>
<div class="clear">
    <a class="ajax link add" href="<?php echo $this->baseUrl('/questionnaire-question/add/questionnaire_id/' . $this->questionnaire['id']) ?>" title="<?php echo t('add existing question'); ?>">
        <?php echo t('add existing question'); ?>
    </a>
</div>
<div class="clear">
    <a class="link add add_page" href="#" title="<?php echo t('add age'); ?>">
        <?php echo t('add page'); ?>
    </a>
</div>

<div class="tabs">
    <ul>
        <?php for($page = 1; $page <= $this->totalPages; $page++): ?>
            <li>
                <a href="#page-<?php echo $page; ?>">
                    <?php echo t('page') . " $page"; ?>
                </a>
            </li>
        <?php endfor; ?>
    </ul>
    <?php for($page = 1; $page <= $this->totalPages; $page++): ?>
        <div id="page-<?php echo $page; ?>">
            <a href="#" class="delete-page link delete"><?php echo t('Delete this page'); ?></a>
            <div class="questions">
                <ul class="questions-list sortable droppable">
                    <?php
                        foreach ($this->questionnaire->QuestionnaireQuestion as $qq) {
                            if (($page === 1 && !isset($qq['CollectionPresentation'][0])) ||
                                $page === (int) $qq['CollectionPresentation'][0]['page'])
                            {
                                echo $this->questionElement($qq, $this->totalPages)->render();
                            }
                        }
                    ?>
                </ul>
            </div>
        </div>
    <?php endfor; ?>
</div>